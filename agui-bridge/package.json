{
  "name": "agui-bridge",
  "version": "1.0.0",
  "type": "module",
  "description": "AG-UI Bridge - Translates AG-UI protocol events ↔ Bindu A2A JSON-RPC",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "bun x tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}